fun main(){
    var name = "Rustam"
    println(name.length)// Узнать длинну строки
    println(name.get(0))// Выве
    println(name.lastIndex) // Удаление последнего индекса
    println(name.toCharArray())// Преобразование переменной в тип Char
    println(name.indices)// Промежуток строки
    println(name.javaClass)
}